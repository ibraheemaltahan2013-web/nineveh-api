{
  "installCommand": "npm install --cache D:/npm-cache",
  "buildCommand": "npx prisma generate",
  "outputDirectory": "public",
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.js" }
  ]
}
